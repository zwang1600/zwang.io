import 'package:flutter/material.dart';

class PorjectDetailsWidget extends StatelessWidget {
  const PorjectDetailsWidget({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}